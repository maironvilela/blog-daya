import { Meta, Story } from '@storybook/react';
import { Search } from '.';

export default {
  title: 'component/Search',
  component: Search
} as Meta;

export const Basic: Story = () => <Search />;

